name: Android tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  compile:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: weddell-seal-android

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      # Compiles main, JVM unit tests, and instrumented tests without an emulator.
      # testDebugUnitTest already compiles unit tests, but androidTest only compiled
      # during connectedDebugAndroidTest.
      - name: Compile app and all test sources
        run: >
          ./gradlew
          compileDebugSources
          compileDebugUnitTestSources
          compileDebugAndroidTestSources
          --stacktrace

  unit-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: weddell-seal-android

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Run unit tests
        run: ./gradlew testDebugUnitTest --stacktrace

      - name: Upload test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-reports
          path: weddell-seal-android/app/build/reports/tests/

  instrumented-tests:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: weddell-seal-android

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Run instrumented tests
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          arch: x86_64
          target: google_apis
          # Default AVD is 320x640; this app is tablet-first and LookupCard is unusable at that size.
          profile: Nexus 6
          disable-animations: true
          # Hardware keyboard keeps the IME from covering Search / Save on the emulator.
          enable-hw-keyboard: true
          ram-size: 2048M
          working-directory: weddell-seal-android
          script: ./gradlew connectedDebugAndroidTest --stacktrace

      - name: Upload instrumented test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: instrumented-test-reports
          path: |
            weddell-seal-android/app/build/reports/androidTests/
            weddell-seal-android/app/build/outputs/androidTest-results/
